# transaction-service

Handles deposits, withdrawals, transfers, and balance lookups against an in-memory account store.
Java with JUnit 5 + Mockito for tests.
Current coverage: 0% (no tests written yet).
