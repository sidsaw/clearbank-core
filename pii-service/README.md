# pii-service

Masks SSNs and account numbers, validates emails, and redacts records before downstream use.
Python (standard library only); no test framework configured.
Current coverage: 0% (no tests written yet).
